package enums;

// Type of buttons in the system
public enum ButtonType {
    FLOOR_BUTTON,       // Buttons inside the elevator
    CALL_UP_BUTTON,     // Call up button on floor
    CALL_DOWN_BUTTON    // Call down button on floor
}
